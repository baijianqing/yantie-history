# MetaOS Alpha 协作规则

本文件约束后续 Codex/Agent 在本仓库中的工作方式。阶段0之后，任何业务实现都必须遵守这里的边界。

## 项目目标

MetaOS Alpha 是“个人意图操作系统”，核心链路是：

意图 -> 主题 -> 知识 -> 证据 -> 判断 -> 行动 -> 复盘

它不是通用知识库、聊天机器人或新闻聚合器。系统必须帮助用户约束注意力、筛选信息、形成可验证判断并推动行动。

## 当前阶段门禁

当前只允许完成阶段0：

- 仓库审计。
- 架构文档。
- 公共 Schema、数据库模型、API 契约的设计说明。
- 后续任务拆分。

禁止在阶段0做业务实现、重构、迁移、依赖升级或运行态数据改造。

## 不可变原则

1. 意图是最高约束，问题只是输入。
2. 主题必须在运行时生成 `ThemeSpec`，不得为每个主题增加 Python 分支。
3. 新主题不得触发重新切块或重建索引。
4. 知识入库后必须形成稳定底座：原文、结构、Chunk、实体、事件、主张、摘要、引用。
5. 模型负责理解问题、规划研究和提出候选；知识库负责提供证据；审计模块负责反证和引用检查。
6. 输出必须区分原文事实、模型推断、争议观点和个人反思。
7. 每次研究最终必须形成 `Action`、明确“不行动”或进入复盘。
8. 使用模块化单体和独立 Worker，不做微服务。
9. 新技术必须由评测结果驱动。

## 任务边界

一个 Codex 任务只解决一个问题。

每个实现任务最多修改：

- 一个业务模块。
- 一个测试目录。
- 与该任务直接相关的一份文档。

除非任务明确要求，否则禁止修改：

- `pyproject.toml`
- 迁移头或迁移基线
- 公共 Schema
- `AGENTS.md`
- 根配置文件
- 运行态 `library/` 数据

## 并行 Agent 规则

- 并行 Agent 不超过 3 个。
- 禁止多个 Agent 同时修改公共 Schema、迁移、根配置、`AGENTS.md`。
- 若多个任务依赖同一接口，先完成契约任务，再并行实现。
- Agent 之间通过文档化接口协作，不通过口头约定共享隐式字段。

## 必填任务说明

每个任务必须写明：

- 任务 ID。
- 价值。
- 依赖。
- 允许修改范围。
- 禁止修改范围。
- 输入。
- 输出。
- 接口。
- 验收标准。
- 测试命令。
- 回滚方式。
- 文档更新。

若某项写不清楚，不得开始实现。

## 测试要求

所有新增能力必须有自动化测试。最低要求：

- Pydantic Schema 有校验测试。
- 数据库读写有 repository 测试。
- API 有契约测试。
- Worker 有幂等和失败路径测试。
- 检索有固定 fixture 的回归测试。
- 模型输出必须结构化，并通过 Schema 校验。

不得破坏现有：

- RAG。
- Streamlit。
- Redis/RQ。
- OCR/PDF 入库。
- 视频生成能力。

## 技术取舍规则

优先使用当前模块化单体和独立 Worker。

近期可新增：

- SQLite FTS5 或 PostgreSQL 全文检索。
- RRF。
- Prompt 版本。
- 索引版本。
- 引用审计。

后置可选：

- PyTorch：仅用于本地重排、NER、指代消解或微调，并作为独立 Model Worker。
- LangGraph：流程稳定后再评估。
- OpenSearch：数据规模增长后再评估。
- Neo4j：出现复杂图查询后再评估。

## 文档优先

公共 Schema、数据库模型和 API 契约必须先文档化，再实现。

当代码与文档冲突时：

- 阶段0以文档为目标设计，不代表已实现。
- 阶段1之后，代码变更必须同步更新对应文档。

## 提交前检查

每个任务完成前至少执行：

- 与任务相关的最小测试。
- `python -m pytest test` 或说明无法执行的原因。
- 若涉及 API，执行对应契约测试。
- 若涉及检索，执行回归检索测试。
- 若涉及 UI，人工或自动截图检查关键路径。

