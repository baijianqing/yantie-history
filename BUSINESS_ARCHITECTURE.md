# MetaOS Alpha 业务架构

## 业务定位

MetaOS Alpha 服务于一个核心问题：

用户如何把长期意图转化为每日注意力选择、可验证研究判断、明确行动和复盘改进。

系统不追求回答更多问题，而是约束哪些问题值得进入研究、哪些信息值得看、哪些结论有证据、哪些行动应该发生。

## 业务对象分层

### 用户主权层

用户主权层回答“什么值得我注意”。

核心对象：

- `CognitiveConstitution`：用户认知宪法，记录长期原则、价值约束、禁区和判断标准。
- `Intent`：阶段性意图，通常按季度或项目周期维护。
- `CurrentRole`：当前角色，例如开发者、创始人、研究者、创作者。
- `AttentionBudget`：每日或每周注意力预算。
- `NotToDoItem`：明确不做、暂缓或不再追逐的事项。

业务规则：

- `Intent` 优先级高于单次问题。
- `AttentionBudget` 限制研究、推荐和内容消费成本。
- `NotToDoItem` 可以阻止议题进入研究。

### 每日认知账本

每日认知账本回答“今天发生了什么，我如何消耗了注意力”。

核心对象：

- `DailyPlan`
- `WorkEvent`
- `Advice`
- `Decision`
- `Action`
- `AttentionDrift`
- `DailyReview`
- `DailySummary`

采集来源：

- 用户手工输入。
- Git commit。
- Markdown 文件变更。
- 研究结果。
- 行动状态。

业务规则：

- 每日账本不是日记，而是意图和行动的证据账。
- `DailySummary` 必须区分事实记录、判断、反思和下一步行动。

### 议题编译器

议题编译器回答“这个问题应该如何被研究”。

输入：

- 用户自然语言问题。
- 当前 `Intent`。
- 当前角色。
- 注意力预算。
- 不做清单。
- 可用知识范围。

输出：

- `ResearchTask`
- `CognitiveOperator`
- `ThemeSpec`
- `EvidenceRequirement`
- `ResearchScope`

业务规则：

- 主题在运行时生成 `ThemeSpec`。
- 不允许为“功高震主”“小人得志陷害忠良”“听信谗言”“功成身退”“角色转换失败”等主题新增 Python 分支。
- 新主题只能改变 `ThemeSpec`、检索条件和证据要求。

### 认知算子

认知算子回答“这类问题需要什么研究动作”。

首版算子：

- `fact_lookup`：查明事实。
- `enumerate_pattern`：枚举模式。
- `compare`：比较对象。
- `causal_analysis`：分析因果链。
- `decision_support`：辅助决策。
- `reflection`：复盘和自我修正。
- `recommend`：有限推荐。

业务规则：

- 算子是研究方法，不是主题分支。
- 算子决定证据矩阵结构、反证策略和输出类型。

### 知识底座

知识底座回答“哪些材料可以作为证据”。

核心对象：

- `Source`
- `DocumentVersion`
- `Chunk`
- `Entity`
- `EntityAlias`
- `Event`
- `Claim`
- `Relationship`
- `Citation`

业务规则：

- 入库后形成稳定底座。
- 新主题不得触发重切块或重建索引。
- 引用必须能回链原文。

### 检索系统

检索系统回答“哪些证据与当前研究任务相关”。

检索路径：

- 向量检索。
- 全文检索。
- 元数据过滤。
- RRF 融合。
- 可选重排。

业务规则：

- 检索结果必须保留引用回链。
- 检索结果不是结论，只是候选证据。

### 研究执行器

研究执行器回答“证据如何支撑或反驳判断”。

过程：

1. 召回候选。
2. 生成证据矩阵。
3. 检测缺失证据。
4. 补充检索。
5. 查找反证。
6. 对证据评分。
7. 分类输出事实、推断、争议和反思。
8. 生成带引用回答。

业务规则：

- 没有证据的结论必须标记为模型推断或个人反思。
- 关键判断必须有引用或明确说明证据不足。

### 御史台

御史台回答“这次研究是否可靠和值得花费”。

检查项：

- 引用是否存在。
- 引用是否支持对应结论。
- 研究范围是否越界。
- 是否缺少反证。
- 证据是否完整。
- 是否存在确认偏误。
- 成本是否超过预算。

输出：

- `AuditReport`
- 风险等级。
- 必须修复项。
- 可接受残余风险。

### 三部有限推荐

三部推荐回答“今天是否有必要把外部信息带入注意力”。

部门：

- 技术部。
- 认知部。
- 商业部。

业务规则：

- 每部每日最多 3 条。
- 全部加总每日最多 5 条。
- 无高价值内容时返回“今日无事上奏”。
- 每条推荐必须说明目标关联、阅读成本、不读损失、建议行动和有效期。

### 宰相

宰相回答“今天到底该做什么，不该做什么”。

输入：

- 当前 `Intent`。
- 当前角色。
- `AttentionBudget`。
- 昨日复盘。
- 今日计划。
- 研究结果。
- 推荐候选。

输出：

- 今日重点。
- 暂缓事项。
- 应忽略事项。
- 认知陷阱提醒。

业务规则：

- 宰相不是推荐流，而是注意力约束器。
- 宰相必须能输出“无新增建议”。

### 内容工坊

内容工坊回答“如何把每日认知账本转成可审核视频资产”。

流程：

`DailySummary` -> `EpisodeSpec` -> 脚本 -> 旁白 -> 字幕 -> 图卡 -> 人工审核 -> MP4

业务规则：

- 视频事实优先引用当日记录、Git commit、文档和人工输入。
- 书籍材料只作为解释框架，不作为虚构个人经历的来源。
- MP4 导出前必须有人审状态。

## 端到端闭环

Alpha 完整闭环：

1. 季度意图设定。
2. 宰相生成今日重点。
3. 用户执行研究或开发。
4. 研究执行器产生证据与结论。
5. 御史台审计研究可靠性。
6. 系统生成 Action 或明确“不行动”。
7. 每日复盘记录实际结果。
8. 内容工坊生成视频。
9. 周报回看意图、行动和复盘。
10. 调整下一轮意图、角色和注意力预算。

