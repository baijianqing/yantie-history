# YT-A2-E2E-001 盐铁会议 A2 静态发布门报告

状态：`blocked_deploy_pending`

审查日期：`2026-07-14`

## 1. 范围

本报告判断《盐铁会议：西汉朝堂复原》A2 当前是否可以作为最新 GitHub Pages 版本发布。

本轮只记录发布门结论，不修改 UI、证据包、API、外部 adapter、验收脚本、部署配置或运行态数据。

## 2. 当前结论

当前本地静态包已经具备发布候选资格，但当前线上 GitHub Pages 不能宣布为最新版本发布通过；换句话说，当前线上 Pages 不能宣布为最新版本发布通过。

结论拆开看：

- 本地 `docs/yantie/` 静态包：`passed_static_candidate`
- 当前线上 Pages：`blocked_stale_deployment`
- A2 静态发布门：`blocked_deploy_pending`
- 最终产品发布：`not_authorized`

这意味着：项目可以继续推进到部署与线上验收，但现在不应把现有线上地址当作最新 A2 成果对外宣布。

## 3. 本地静态包验收

本地静态包验收已通过：

```text
Yantie acceptance: 36/36 checks passed.
Manual review recommended: desktop/power-silence, mobile/power-silence, reduced-motion/power-silence
```

本地盐铁相关测试已通过：

```text
87 passed
```

全量回归已通过：

```text
382 passed
```

本地结论：`docs/yantie/` 当前适合作为 GitHub Pages 静态预览候选包。

## 4. 线上 Pages 验收

当前线上地址：

```text
https://baijianqing.github.io/yantie-history/yantie/
```

线上验收未通过最新 A2 矩阵：

```text
locator.click: Timeout 30000ms exceeded.
waiting for locator('[data-post-court-action="material-guide"]')
```

判断：线上 Pages 很可能仍是旧版本，尚未包含当前 A2 的退朝后材料导览入口，因此不能代表当前分支的最新状态。

## 5. 剩余人工复核

自动验收仍将 `power-silence` 标记为人工复核项：

- `desktop/power-silence`
- `mobile/power-silence`
- `reduced-motion/power-silence`

原因：霍光沉默场景的重点是留白、节奏和权力压迫感。自动化可以确认入口、结构和无遮挡，但不能判断这一幕的观感是否成立。

人工复核完成前，不应把 A2 标记为完整发布通过。

## 6. 发布判断

当前适合：

- 作为本地静态预览候选继续推进。
- 推送到发布分支后进行线上复验。
- 小范围内部检查 A2 材料增强体验。

当前不适合：

- 直接宣布现有线上 Pages 已是最新 A2 版本。
- 作为最终产品版发布。
- 作为生产级公开发布。

## 7. 解除阻断的下一步

建议按以下顺序推进：

1. 推送当前分支或将当前 `docs/yantie/` 发布到 GitHub Pages 对应分支。
2. 等待 Pages 部署完成后，对线上 URL 重新运行静态验收。
3. 对 `power-silence` 三种视口做人工观感复核。
4. 若线上验收通过且人工复核无阻断问题，将发布门状态更新为 `passed_static_preview`。
5. 另开任务处理 Playwright 验收依赖可复现问题，避免只能依赖 Codex 工作区绑定 Node 环境。

## 8. 下一步任务建议

优先任务：

- `YT-A2-DEPLOY-001`：发布当前静态包到 GitHub Pages。
- `YT-A2-E2E-002`：部署后线上验收与截图复核。
- `YT-A2-REL-002`：Playwright 验收依赖可复现说明或脚本包装。

后续再推进：

- `YT-A2-MAIN-001`：决定哪些 A2 材料可进入首次主体验轻提示。
- `YT-A2-DOC-001`：整理面向普通用户的发布说明和 README。

## 9. 总结

当前项目不是“不适合发布”，而是“本地静态预览候选已经准备好，线上发布门还没过”。

```text
YT-A2-E2E-001 = blocked_deploy_pending
```

下一步应优先完成部署和线上复验，而不是继续增加新材料或扩大功能面。
